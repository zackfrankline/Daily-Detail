{
  "name": "razorpay-server",
  "version": "1.0.0",
  "description": "payment handling express server as firebase cloud function ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js",
    "serve": "firebase emulators:start --only functions",
    "start-with-data": "firebase emulators:start --import=firestore-emulator-data --export-on-exit=firestore-emulator-data",
    "shell": "firebase functions:shell",
    "deploy": "firebase deploy --only functions:api",
    "logs": "firebase functions:log"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "firebase-admin": "^12.2.0",
    "firebase-functions": "^5.0.1",
    "nodemon": "^3.1.4",
    "razorpay": "^2.9.4"
  },
  "engines": {
    "node": "18"
  },
  "private": true
}
